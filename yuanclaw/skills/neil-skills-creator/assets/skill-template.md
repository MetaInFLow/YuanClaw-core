---
name: <skill-name>
description: <Skill 核心价值，以及适用场景/用户意图（可列举多个，互不耦合）>
dependency:                       # 选填 - 依赖管理
  python: # 参考python requirement.txt的格式列举scripts脚本的依赖包及版本
    - package-name==version
    - another-package>=1.2.0
  system: # 前置需要执行的命令行命令，例如「Skill固定结构」外的非标准文件/文件夹的创建等。当前路径视为相对于Skill目录的父目录。注意:此字段仅用于系统级命令(如mkdir、chmod等)，禁止包含Python包安装命令。
    - <command>
---

# <Skill 标题>

## 任务目标
- 本 Skill 用于:<一句话场景>
- 能力包含:<核心能力要点>
- 触发条件:<典型用户表达或上下文信号>

## 工作流程
（流程总览，可用流程图或文字描述）

## 操作步骤

### 第一步：<步骤名称>
- 输入说明
- 处理方式（智能体处理或调用脚本）
- 输出说明

### 第二步：<步骤名称>
（同上结构）

### 第N步：<步骤名称>
（同上结构）

## 资源索引
- 必要脚本:见 [scripts/<script-name>.py](scripts/<script-name>.py)(用途与参数:<一句话>)
- 领域参考:见 [references/<topic>.md](references/<topic>.md)(何时读取:<触发/场景>)
- 输出资产:见 [assets/<template-dir>/](assets/<template-dir>/)(直接用于生成/修饰输出)

## 质量检查清单
- [ ] <检查项1>
- [ ] <检查项2>
- [ ] <检查项3>

## 注意事项
- 仅在需要时读取参考，保持上下文简洁
- 当操作脆弱或需强一致性时，优先调用脚本执行并进行结果校验
- 充分利用智能体的语言理解和生成能力，避免为简单任务编写脚本

## 边界说明
- 能做什么
- 不能做什么

## 使用示例

### 示例1：<场景名称>
**用户需求**：<用户的具体需求>
**执行方式**：<脚本/智能体/混合>
**关键参数**：<重要的参数或配置>
**执行结果**：<输出结果的描述>
